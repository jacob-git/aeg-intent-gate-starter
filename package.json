{
  "name": "aeg-intent-gate-starter",
  "version": "0.1.0",
  "private": true,
  "description": "Starter app for gating AI tool calls with human approval.",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs"
  },
  "dependencies": {
    "@pallattu/aeg-intent-gate": "^0.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
